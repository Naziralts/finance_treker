import 'package:flutter/material.dart';

class AppColors {
  // Primary Banking Gradient
  static const primaryStart = Color(0xFF6C63FF);
  static const primaryEnd = Color(0xFF5A54E6);

  // Background
  static const lightBackground = Color(0xFFF5F6FA);
  static const darkBackground = Color(0xFF1E1E2E);

  // Income / Expense
  static const income = Color(0xFF4CAF50);
  static const expense = Color(0xFFE53935);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);

  // Card
  static const cardLight = Colors.white;
  static const cardDark = Color(0xFF2A2A3D);
}
